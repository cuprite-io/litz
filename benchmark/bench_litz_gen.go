//go:build amd64 || arm64 || 386 || arm || wasm || mips64le || ppc64le || riscv64

// Code generated by litz-gen. DO NOT EDIT.
package benchmark

import (
	"strings"
	"unsafe"

	"github.com/cuprite-io/litz"
)

// smallLitzPayloadLitzMirror is the GC-safe layout mirror for SmallLitzPayload
type smallLitzPayloadLitzMirror struct {
	ID uint64
}

// Compile-time size assertions to guarantee layout identity
var _ [int(unsafe.Sizeof(SmallLitzPayload{})) - int(unsafe.Sizeof(smallLitzPayloadLitzMirror{}))]byte
var _ [int(unsafe.Sizeof(smallLitzPayloadLitzMirror{})) - int(unsafe.Sizeof(SmallLitzPayload{}))]byte
var _ [int(unsafe.Offsetof(SmallLitzPayload{}.ID)) - int(unsafe.Offsetof(smallLitzPayloadLitzMirror{}.ID))]byte
var _ [int(unsafe.Offsetof(smallLitzPayloadLitzMirror{}.ID)) - int(unsafe.Offsetof(SmallLitzPayload{}.ID))]byte

func MarshalSmallLitzPayload(u *SmallLitzPayload, buf []byte) ([]byte, error) {
	fixedSize := int(unsafe.Sizeof(smallLitzPayloadLitzMirror{}))
	totalSize := 8 + fixedSize

	// Ensure output buffer is aligned and large enough
	if cap(buf) < totalSize || (cap(buf) > 0 && uintptr(unsafe.Pointer(&buf[0]))%8 != 0) {
		buf = litz.AlignedBuffer(totalSize)
	}
	buf = buf[:totalSize]

	// Write 8-byte format signature: 'LTZ0' + Version 1
	*(*[4]byte)(unsafe.Pointer(&buf[0])) = [4]byte{'L', 'T', 'Z', '0'}
	buf[4] = 1
	buf[5] = 0
	buf[6] = 0
	buf[7] = 0

	// Write directly to the buffer's mirror pointer (O1 Optimization: No stack allocation/double-copy!)
	// Zero the fixed size header to prevent padding data leaks (Issue 1)
	*(*smallLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8])) = smallLitzPayloadLitzMirror{}
	mirror := (*smallLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	mirror.ID = u.ID
	return buf, nil
}

// UnmarshalSmallLitzPayload deserializes the buffer using SBC-IPR.
// WARNING: Unmarshaled strings and slices point directly into buf.
// The input buf MUST outlive the returned structure u to prevent use-after-free corruption.
func UnmarshalSmallLitzPayload(buf []byte, u *SmallLitzPayload) error {
	if len(buf) < 8 {
		return litz.ErrBufferTooShort
	}
	if *(*[4]byte)(unsafe.Pointer(&buf[0])) != [4]byte{'L', 'T', 'Z', '0'} || buf[4] != 1 {
		return litz.ErrInvalidHeader
	}
	mirror := (*smallLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
	}

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
		u.ID = mirror.ID
	}

	return nil
}

// Clone creates a deep-copy of SmallLitzPayload, allocating fresh memory for all string and slice fields.
func (u *SmallLitzPayload) Clone() *SmallLitzPayload {
	res := new(SmallLitzPayload)
	res.ID = u.ID
	return res
}

// mediumLitzPayloadLitzMirror is the GC-safe layout mirror for MediumLitzPayload
type mediumLitzPayloadLitzMirror struct {
	ID    uint64
	Email struct {
		offset uintptr
		length int
	}
	Active bool
	Clicks uint32
}

// Compile-time size assertions to guarantee layout identity
var _ [int(unsafe.Sizeof(MediumLitzPayload{})) - int(unsafe.Sizeof(mediumLitzPayloadLitzMirror{}))]byte
var _ [int(unsafe.Sizeof(mediumLitzPayloadLitzMirror{})) - int(unsafe.Sizeof(MediumLitzPayload{}))]byte
var _ [int(unsafe.Offsetof(MediumLitzPayload{}.ID)) - int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.ID))]byte
var _ [int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.ID)) - int(unsafe.Offsetof(MediumLitzPayload{}.ID))]byte
var _ [int(unsafe.Offsetof(MediumLitzPayload{}.Email)) - int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Email))]byte
var _ [int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Email)) - int(unsafe.Offsetof(MediumLitzPayload{}.Email))]byte
var _ [int(unsafe.Offsetof(MediumLitzPayload{}.Active)) - int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Active))]byte
var _ [int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Active)) - int(unsafe.Offsetof(MediumLitzPayload{}.Active))]byte
var _ [int(unsafe.Offsetof(MediumLitzPayload{}.Clicks)) - int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Clicks))]byte
var _ [int(unsafe.Offsetof(mediumLitzPayloadLitzMirror{}.Clicks)) - int(unsafe.Offsetof(MediumLitzPayload{}.Clicks))]byte

func MarshalMediumLitzPayload(u *MediumLitzPayload, buf []byte) ([]byte, error) {
	fixedSize := int(unsafe.Sizeof(mediumLitzPayloadLitzMirror{}))
	totalSize := 8 + fixedSize

	EmailLen := len(u.Email)
	if totalSize+EmailLen < totalSize {
		return nil, litz.ErrSizeOverflow
	}
	totalSize += EmailLen

	// Ensure output buffer is aligned and large enough
	if cap(buf) < totalSize || (cap(buf) > 0 && uintptr(unsafe.Pointer(&buf[0]))%8 != 0) {
		buf = litz.AlignedBuffer(totalSize)
	}
	buf = buf[:totalSize]

	// Write 8-byte format signature: 'LTZ0' + Version 1
	*(*[4]byte)(unsafe.Pointer(&buf[0])) = [4]byte{'L', 'T', 'Z', '0'}
	buf[4] = 1
	buf[5] = 0
	buf[6] = 0
	buf[7] = 0

	// Write directly to the buffer's mirror pointer (O1 Optimization: No stack allocation/double-copy!)
	// Zero the fixed size header to prevent padding data leaks (Issue 1)
	*(*mediumLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8])) = mediumLitzPayloadLitzMirror{}
	mirror := (*mediumLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	mirror.ID = u.ID
	mirror.Active = u.Active
	mirror.Clicks = u.Clicks
	currentOffset := uintptr(8 + fixedSize)

	mirror.Email.offset = currentOffset
	mirror.Email.length = EmailLen
	copy(buf[currentOffset:], u.Email)
	currentOffset += uintptr(EmailLen)

	return buf, nil
}

// UnmarshalMediumLitzPayload deserializes the buffer using SBC-IPR.
// WARNING: Unmarshaled strings and slices point directly into buf.
// The input buf MUST outlive the returned structure u to prevent use-after-free corruption.
func UnmarshalMediumLitzPayload(buf []byte, u *MediumLitzPayload) error {
	if len(buf) < 8 {
		return litz.ErrBufferTooShort
	}
	if *(*[4]byte)(unsafe.Pointer(&buf[0])) != [4]byte{'L', 'T', 'Z', '0'} || buf[4] != 1 {
		return litz.ErrInvalidHeader
	}
	mirror := (*mediumLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
	}
	if 8+unsafe.Offsetof(mirror.Email)+unsafe.Sizeof(mirror.Email) <= uintptr(len(buf)) {
		if mirror.Email.offset > uintptr(len(buf)) || mirror.Email.length < 0 || mirror.Email.length > len(buf)-int(mirror.Email.offset) {
			return litz.ErrStringOutOfBounds
		}
	}
	if 8+unsafe.Offsetof(mirror.Active)+unsafe.Sizeof(mirror.Active) <= uintptr(len(buf)) {
	}
	if 8+unsafe.Offsetof(mirror.Clicks)+unsafe.Sizeof(mirror.Clicks) <= uintptr(len(buf)) {
	}

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
		u.ID = mirror.ID
	}
	if 8+unsafe.Offsetof(mirror.Active)+unsafe.Sizeof(mirror.Active) <= uintptr(len(buf)) {
		u.Active = mirror.Active
	}
	if 8+unsafe.Offsetof(mirror.Clicks)+unsafe.Sizeof(mirror.Clicks) <= uintptr(len(buf)) {
		u.Clicks = mirror.Clicks
	}
	if 8+unsafe.Offsetof(mirror.Email)+unsafe.Sizeof(mirror.Email) <= uintptr(len(buf)) {
		if mirror.Email.length > 0 {
			u.Email = unsafe.String((*byte)(unsafe.Add(unsafe.Pointer(&buf[0]), mirror.Email.offset)), mirror.Email.length)
		} else {
			u.Email = ""
		}
	}

	return nil
}

// Clone creates a deep-copy of MediumLitzPayload, allocating fresh memory for all string and slice fields.
func (u *MediumLitzPayload) Clone() *MediumLitzPayload {
	res := new(MediumLitzPayload)
	res.ID = u.ID
	res.Email = strings.Clone(u.Email)
	res.Active = u.Active
	res.Clicks = u.Clicks
	return res
}

// largeLitzPayloadLitzMirror is the GC-safe layout mirror for LargeLitzPayload
type largeLitzPayloadLitzMirror struct {
	ID   uint64
	Name struct {
		offset uintptr
		length int
	}
	Active  bool
	Numbers struct {
		offset   uintptr
		length   int
		capacity int
	}
	Tags struct {
		offset   uintptr
		length   int
		capacity int
	}
	Child uintptr
}

// Compile-time size assertions to guarantee layout identity
var _ [int(unsafe.Sizeof(LargeLitzPayload{})) - int(unsafe.Sizeof(largeLitzPayloadLitzMirror{}))]byte
var _ [int(unsafe.Sizeof(largeLitzPayloadLitzMirror{})) - int(unsafe.Sizeof(LargeLitzPayload{}))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.ID)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.ID))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.ID)) - int(unsafe.Offsetof(LargeLitzPayload{}.ID))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.Name)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Name))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Name)) - int(unsafe.Offsetof(LargeLitzPayload{}.Name))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.Active)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Active))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Active)) - int(unsafe.Offsetof(LargeLitzPayload{}.Active))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.Numbers)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Numbers))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Numbers)) - int(unsafe.Offsetof(LargeLitzPayload{}.Numbers))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.Tags)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Tags))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Tags)) - int(unsafe.Offsetof(LargeLitzPayload{}.Tags))]byte
var _ [int(unsafe.Offsetof(LargeLitzPayload{}.Child)) - int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Child))]byte
var _ [int(unsafe.Offsetof(largeLitzPayloadLitzMirror{}.Child)) - int(unsafe.Offsetof(LargeLitzPayload{}.Child))]byte

func MarshalLargeLitzPayload(u *LargeLitzPayload, buf []byte) ([]byte, error) {
	fixedSize := int(unsafe.Sizeof(largeLitzPayloadLitzMirror{}))
	totalSize := 8 + fixedSize

	NameLen := len(u.Name)
	if totalSize+NameLen < totalSize {
		return nil, litz.ErrSizeOverflow
	}
	totalSize += NameLen
	NumbersLen := len(u.Numbers)
	var NumbersDummy int64
	NumbersSize := NumbersLen * int(unsafe.Sizeof(NumbersDummy))
	if NumbersLen > 0 && NumbersSize/NumbersLen != int(unsafe.Sizeof(NumbersDummy)) {
		return nil, litz.ErrSizeOverflow
	}
	if totalSize+NumbersSize < totalSize {
		return nil, litz.ErrSizeOverflow
	}
	totalSize += NumbersSize
	TagsLen := len(u.Tags)
	// Slice array of string headers (size of Go string header)
	TagsHeaderSize := TagsLen * int(unsafe.Sizeof(""))
	if TagsLen > 0 && TagsHeaderSize/TagsLen != int(unsafe.Sizeof("")) {
		return nil, litz.ErrSizeOverflow
	}
	if totalSize+TagsHeaderSize < totalSize {
		return nil, litz.ErrSizeOverflow
	}
	totalSize += TagsHeaderSize
	// Add string backing bytes
	for i := 0; i < TagsLen; i++ {
		strLen := len(u.Tags[i])
		if totalSize+strLen < totalSize {
			return nil, litz.ErrSizeOverflow
		}
		totalSize += strLen
	}
	var ChildBytes []byte
	if u.Child != nil {
		var err error
		ChildBytes, err = MarshalMediumLitzPayload(u.Child, nil)
		if err != nil {
			return nil, err
		}
	}
	ChildLen := len(ChildBytes)
	if totalSize+ChildLen < totalSize {
		return nil, litz.ErrSizeOverflow
	}
	totalSize += ChildLen

	// Ensure output buffer is aligned and large enough
	if cap(buf) < totalSize || (cap(buf) > 0 && uintptr(unsafe.Pointer(&buf[0]))%8 != 0) {
		buf = litz.AlignedBuffer(totalSize)
	}
	buf = buf[:totalSize]

	// Write 8-byte format signature: 'LTZ0' + Version 1
	*(*[4]byte)(unsafe.Pointer(&buf[0])) = [4]byte{'L', 'T', 'Z', '0'}
	buf[4] = 1
	buf[5] = 0
	buf[6] = 0
	buf[7] = 0

	// Write directly to the buffer's mirror pointer (O1 Optimization: No stack allocation/double-copy!)
	// Zero the fixed size header to prevent padding data leaks (Issue 1)
	*(*largeLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8])) = largeLitzPayloadLitzMirror{}
	mirror := (*largeLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	mirror.ID = u.ID
	mirror.Active = u.Active
	currentOffset := uintptr(8 + fixedSize)

	mirror.Name.offset = currentOffset
	mirror.Name.length = NameLen
	copy(buf[currentOffset:], u.Name)
	currentOffset += uintptr(NameLen)

	mirror.Numbers.offset = currentOffset
	mirror.Numbers.length = NumbersLen
	mirror.Numbers.capacity = cap(u.Numbers)
	if NumbersLen > 0 {
		varSize := NumbersLen * int(unsafe.Sizeof(NumbersDummy))
		sliceBytes := unsafe.Slice((*byte)(unsafe.Pointer(&u.Numbers[0])), varSize)
		copy(buf[currentOffset:], sliceBytes)
		currentOffset += uintptr(varSize)
	}

	mirror.Tags.offset = currentOffset
	mirror.Tags.length = TagsLen
	mirror.Tags.capacity = TagsLen
	if TagsLen > 0 {
		// First reserve space for slice of string headers (16B each)
		strArrayOffset := currentOffset
		currentOffset += uintptr(TagsLen * 16)
		// Write string bytes and fill array headers
		for i := 0; i < TagsLen; i++ {
			strLen := len(u.Tags[i])
			copy(buf[currentOffset:], u.Tags[i])
			// Fill header in slice array
			entryPtr := strArrayOffset + uintptr(i*16)
			*(*uintptr)(unsafe.Pointer(&buf[entryPtr])) = currentOffset
			*(*int)(unsafe.Pointer(&buf[entryPtr+8])) = strLen
			currentOffset += uintptr(strLen)
		}
	}

	if ChildLen > 0 {
		mirror.Child = currentOffset
		copy(buf[currentOffset:], ChildBytes)
		currentOffset += uintptr(ChildLen)
	} else {
		mirror.Child = 0
	}

	return buf, nil
}

// UnmarshalLargeLitzPayload deserializes the buffer using SBC-IPR.
// WARNING: Unmarshaled strings and slices point directly into buf.
// The input buf MUST outlive the returned structure u to prevent use-after-free corruption.
func UnmarshalLargeLitzPayload(buf []byte, u *LargeLitzPayload) error {
	if len(buf) < 8 {
		return litz.ErrBufferTooShort
	}
	if *(*[4]byte)(unsafe.Pointer(&buf[0])) != [4]byte{'L', 'T', 'Z', '0'} || buf[4] != 1 {
		return litz.ErrInvalidHeader
	}
	mirror := (*largeLitzPayloadLitzMirror)(unsafe.Pointer(&buf[8]))

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
	}
	if 8+unsafe.Offsetof(mirror.Name)+unsafe.Sizeof(mirror.Name) <= uintptr(len(buf)) {
		if mirror.Name.offset > uintptr(len(buf)) || mirror.Name.length < 0 || mirror.Name.length > len(buf)-int(mirror.Name.offset) {
			return litz.ErrStringOutOfBounds
		}
	}
	if 8+unsafe.Offsetof(mirror.Active)+unsafe.Sizeof(mirror.Active) <= uintptr(len(buf)) {
	}
	if 8+unsafe.Offsetof(mirror.Numbers)+unsafe.Sizeof(mirror.Numbers) <= uintptr(len(buf)) {
		var NumbersDummy int64
		if mirror.Numbers.offset > uintptr(len(buf)) || mirror.Numbers.length < 0 || mirror.Numbers.length > (len(buf)-int(mirror.Numbers.offset))/int(unsafe.Sizeof(NumbersDummy)) {
			return litz.ErrSliceOutOfBounds
		}
	}
	if 8+unsafe.Offsetof(mirror.Tags)+unsafe.Sizeof(mirror.Tags) <= uintptr(len(buf)) {
		if mirror.Tags.offset > uintptr(len(buf)) || mirror.Tags.length < 0 || mirror.Tags.length > (len(buf)-int(mirror.Tags.offset))/int(unsafe.Sizeof("")) {
			return litz.ErrSliceOutOfBounds
		}
	}
	if 8+unsafe.Offsetof(mirror.Child)+unsafe.Sizeof(mirror.Child) <= uintptr(len(buf)) {
		if mirror.Child > uintptr(len(buf)) || (mirror.Child > 0 && uintptr(len(buf))-mirror.Child < unsafe.Sizeof(mediumLitzPayloadLitzMirror{})) {
			return litz.ErrPointerOutOfBounds
		}
	}

	if 8+unsafe.Offsetof(mirror.ID)+unsafe.Sizeof(mirror.ID) <= uintptr(len(buf)) {
		u.ID = mirror.ID
	}
	if 8+unsafe.Offsetof(mirror.Active)+unsafe.Sizeof(mirror.Active) <= uintptr(len(buf)) {
		u.Active = mirror.Active
	}
	if 8+unsafe.Offsetof(mirror.Name)+unsafe.Sizeof(mirror.Name) <= uintptr(len(buf)) {
		if mirror.Name.length > 0 {
			u.Name = unsafe.String((*byte)(unsafe.Add(unsafe.Pointer(&buf[0]), mirror.Name.offset)), mirror.Name.length)
		} else {
			u.Name = ""
		}
	}
	if 8+unsafe.Offsetof(mirror.Numbers)+unsafe.Sizeof(mirror.Numbers) <= uintptr(len(buf)) {
		if mirror.Numbers.length > 0 {
			u.Numbers = unsafe.Slice((*int64)(unsafe.Add(unsafe.Pointer(&buf[0]), mirror.Numbers.offset)), mirror.Numbers.length)
		} else {
			u.Numbers = nil
		}
	}
	if 8+unsafe.Offsetof(mirror.Tags)+unsafe.Sizeof(mirror.Tags) <= uintptr(len(buf)) {
		if mirror.Tags.length > 0 {
			strHeaders := unsafe.Slice((*struct {
				offset uintptr
				length int
			})(unsafe.Add(unsafe.Pointer(&buf[0]), mirror.Tags.offset)), mirror.Tags.length)
			maxExtent := 0
			for i := 0; i < len(strHeaders); i++ {
				endOffset := int(strHeaders[i].offset) + strHeaders[i].length
				if endOffset > maxExtent {
					maxExtent = endOffset
				}
			}
			if maxExtent > len(buf) {
				return litz.ErrStringOutOfBounds
			}
			slice := make([]string, len(strHeaders))
			for i := 0; i < len(strHeaders); i++ {
				if strHeaders[i].length > 0 {
					slice[i] = unsafe.String((*byte)(unsafe.Add(unsafe.Pointer(&buf[0]), strHeaders[i].offset)), strHeaders[i].length)
				}
			}
			u.Tags = slice
		} else {
			u.Tags = nil
		}
	}
	if 8+unsafe.Offsetof(mirror.Child)+unsafe.Sizeof(mirror.Child) <= uintptr(len(buf)) {
		if mirror.Child > 0 {
			if u.Child == nil {
				u.Child = new(MediumLitzPayload)
			}
			err := UnmarshalMediumLitzPayload(buf[mirror.Child:], u.Child)
			if err != nil {
				return err
			}
		} else {
			u.Child = nil
		}
	}

	return nil
}

// Clone creates a deep-copy of LargeLitzPayload, allocating fresh memory for all string and slice fields.
func (u *LargeLitzPayload) Clone() *LargeLitzPayload {
	res := new(LargeLitzPayload)
	res.ID = u.ID
	res.Name = strings.Clone(u.Name)
	res.Active = u.Active
	if u.Numbers != nil {
		res.Numbers = make([]int64, len(u.Numbers))
		copy(res.Numbers, u.Numbers)
	}
	if u.Tags != nil {
		res.Tags = make([]string, len(u.Tags))
		for i := 0; i < len(u.Tags); i++ {
			res.Tags[i] = strings.Clone(u.Tags[i])
		}
	}
	if u.Child != nil {
		res.Child = u.Child.Clone()
	}
	return res
}
